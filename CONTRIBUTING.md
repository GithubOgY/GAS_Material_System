   git config --global user.name "GithubOgY"
   git config --global user.email "yukayohei@gmail.com"

# コントリビューションガイド

このプロジェクトへの貢献を検討していただき、ありがとうございます。このガイドは、開発に参加する際のルールとベストプラクティスを説明します。

## 🔄 開発フロー

### 1. 機能追加・修正の流れ

1. **要件の確認**
   - 追加・修正する機能を明確にする
   - 既存の機能への影響を確認

2. **コードの実装**
   - `Code.gs`または`Dialog.html`を編集
   - 既存のコードスタイルに従う
   - エラーハンドリングを追加

3. **テスト**
   - 追加・修正した機能をテスト
   - 既存の機能が壊れていないか確認
   - エッジケースをテスト

4. **ドキュメントの更新**
   - `DEVELOPMENT_LOG.md`に変更内容を記録
   - ユーザー向け機能の場合は`USER_MANUAL.md`を更新
   - 必要に応じて`README.md`を更新

5. **コードレビュー**
   - コードの品質を確認
   - エラーハンドリングが適切か確認
   - パフォーマンスへの影響を確認

## 📝 コーディング規約

### 命名規則

#### 関数名
- **メニュー関数**: `menu` + 機能名（例: `menuReceiveMaterial`）
- **ダイアログ関数**: `open` + 機能名 + `Dialog`（例: `openOrderMaterialDialog`）
- **処理関数**: 動詞 + 名詞（例: `processForm`, `registerBOM`）
- **ヘルパー関数**: `get` + 名詞（例: `getStock`, `getBOM`）

#### 定数
- 大文字とアンダースコア（例: `SHEET_NAMES`, `PROP_MODE`）
- 意味のある名前を使用

#### 変数
- キャメルケース（例: `orderQty`, `bomData`）
- 意味のある名前を使用
- 略語は避ける（例: `qty`ではなく`quantity`、ただし既存コードとの一貫性を保つ）

### コードスタイル

#### インデント
- スペース2つを使用（タブは使用しない）

#### コメント
- 複雑なロジックにはコメントを追加
- 関数の目的を説明するコメントを追加
- 日本語でコメントを記述

#### エラーハンドリング
- すべての主要な関数でエラーハンドリングを実装
- ユーザーに分かりやすいエラーメッセージを表示
- `Logger.log()`でデバッグ情報を記録

### ベストプラクティス

1. **数値の扱い**
   - スプレッドシートから取得した値は必ず`Number()`で変換
   - `isNaN()`で数値チェックを実施

2. **null/undefinedチェック**
   - シートや要素が存在するか確認
   - オプショナルチェーン（`?.`）を適切に使用

3. **データ検証**
   - ユーザー入力は必ず検証
   - 不正な値の場合は適切なエラーメッセージを表示

4. **パフォーマンス**
   - 不要なシートアクセスを避ける
   - ループ内でのシート操作を最小限に

## 🧪 テスト

### テスト項目

新しい機能を追加する際は、以下をテストしてください：

1. **正常系**
   - 正常な入力で期待通りに動作するか

2. **異常系**
   - 不正な入力で適切なエラーが表示されるか
   - 空の値で適切に処理されるか

3. **エッジケース**
   - 境界値（0、負の値など）で適切に処理されるか
   - 大量のデータで問題が発生しないか

4. **既存機能への影響**
   - 既存の機能が壊れていないか
   - データの整合性が保たれているか

### テスト方法

1. 手動テスト
   - 各機能を実際に操作して確認
   - ブラウザの開発者ツール（F12）でエラーを確認

2. ログ確認
   - Apps Scriptの実行ログを確認
   - `Logger.log()`で出力した情報を確認

## 📚 ドキュメント

### 必須の更新

機能追加・修正時は、以下を更新してください：

1. **`DEVELOPMENT_LOG.md`**
   - 変更内容を記録
   - 問題点、原因、修正内容を記載
   - 影響範囲を明記

2. **`USER_MANUAL.md`**（ユーザー向け機能の場合）
   - 新しい機能の使い方を追加
   - 既存の手順を更新

3. **`README.md`**（大きな変更の場合）
   - 機能一覧を更新
   - セットアップ手順を更新

### ドキュメントの書き方

- **明確に**: 専門用語を使う場合は説明を追加
- **具体的に**: 手順は具体的に記述
- **例を含める**: 可能な限り例を追加

## 🐛 バグ報告

バグを発見した場合：

1. **再現手順を記録**
   - どの操作で発生するか
   - エラーメッセージの内容

2. **環境情報を記録**
   - ブラウザの種類とバージョン
   - 発生した日時

3. **ログを確認**
   - Apps Scriptの実行ログ
   - ブラウザの開発者ツールのコンソール

4. **`DEVELOPMENT_LOG.md`に記録**
   - 問題の詳細
   - 解決方法（解決した場合）

## 🔒 セキュリティ

- **機密情報**: APIキーやパスワードをコードに含めない
- **入力検証**: ユーザー入力は必ず検証
- **権限**: 最小限の権限で動作するように設計

## 📋 チェックリスト

機能追加・修正前に確認：

- [ ] コードが既存のスタイルに従っている
- [ ] エラーハンドリングが実装されている
- [ ] データ検証が実装されている
- [ ] テストが完了している
- [ ] `DEVELOPMENT_LOG.md`が更新されている
- [ ] 必要に応じて`USER_MANUAL.md`が更新されている
- [ ] コメントが適切に追加されている
- [ ] 既存の機能が壊れていない

## 🤝 コミュニケーション

- 質問や提案がある場合は、`DEVELOPMENT_LOG.md`に記録
- 大きな変更を行う場合は、事前に影響範囲を確認
- コードレビューを実施（可能な場合）

---

**最終更新**: 2024年

